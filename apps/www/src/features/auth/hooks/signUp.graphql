mutation SignUpAnonymously($request: SignUpAnonymouslyRequest!) {
  signUpAnonymously(request: $request) {
    user {
      id
      roles {
        id
      }
    }
    problems {
      message
    }
  }
}
