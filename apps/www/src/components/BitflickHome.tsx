import { FC } from "react";
import { AppLink } from "@/components/AppLink";

export const BitflickHome: FC = () => {
  return (
    <AppLink href="/" underline="none">
      bitflick
    </AppLink>
  );
};
